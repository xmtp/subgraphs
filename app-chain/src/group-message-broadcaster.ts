import {} from '@graphprotocol/graph-ts';

/* ============ Handlers ============ */

// For now, this is too intensive on USDC, but will be feasible on FeeToken/xUSD.
export function handleTransfer(event: void): void {}
